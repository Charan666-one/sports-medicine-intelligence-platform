# Dependencies
node_modules

# Environment / secrets
.env
.env.local
.env.*.local

# Build output
dist
.vite

# Runtime data
uploads
# Legacy SQLite artifacts (project uses PostgreSQL)
*.db
*.db-journal

# OCR model data (downloaded by `npm run setup`)
data/tessdata

# Local Redis persistence snapshot (redis-server run from the repo root)
dump.rdb

# OS / editor cruft
.DS_Store
*.log
